server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        auth_basic "OCCS-SOC — Accès Restreint";
        auth_basic_user_file /etc/nginx/.htpasswd;
        try_files $uri $uri/ =404;
    }
}
